import { RankCalculatorPipe } from './rank-calculator.pipe';

describe('RankCalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new RankCalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
